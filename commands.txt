git init - Initialize git repository
git status - Shows the status of the files in the repository
git add . - Stages all the files
git commit -m "message" - commits all staged files
git reset `hashcode` - Restores to that version
git stash - Moves the changes to stash
git stash pop - Pops the changes from stash
git stash clear - Clears stash